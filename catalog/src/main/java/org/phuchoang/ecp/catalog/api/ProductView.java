package org.phuchoang.ecp.catalog.api;

import java.util.UUID;

/** Sprint 04 (`US-AUD-03`) RBAC wiring demo stub — see {@code catalog.application.GetProductService}. */
public record ProductView(UUID id, String name) {
}
