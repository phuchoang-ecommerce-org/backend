package org.phuchoang.ecp.catalog.application;

import java.util.UUID;

/** Sprint 04 (`US-AUD-03`) RBAC wiring demo stub — see {@link GetProductService}. */
public record ProductView(UUID id, String name) {
}
