package org.phuchoang.ecp.ordering.application;

/**
 * Sprint 05 placeholder shape for one row of `listOrders`' read model — deliberately empty:
 * {@code ordering} has no order-placement code yet (Sprint 18), so nothing populates one. Exists
 * only so {@link OrderPageResult}'s {@code items} is typed rather than {@code List<Object>}.
 */
public record OrderSummary() {
}
