package org.phuchoang.ecp.identity.domain;

/** {@code identity_token.token_type} — the three single-use token kinds this schema carries. */
public enum TokenType {
    EMAIL_VERIFICATION, PASSWORD_RESET, REFRESH
}
